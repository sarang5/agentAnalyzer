{
  "name": "support-call-analyzer",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node app/index.js",
    "test": "vitest",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp app/index.js dist/"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "@types/multer": "^1.4.7",
    "openai": "^4.20.1"
  },
  "devDependencies": {
    "vitest": "^0.34.6"
  }
}